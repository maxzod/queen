name: queen
description: collection of tools for the day to day work as a flutter
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.2
