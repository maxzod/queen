name: example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
    version: ^0.0.0
  nations: 0.0.1
  qcore: 0.0.1
dependency_overrides:
  platform: ^3.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  lint: ^1.0.0

flutter:
  uses-material-design: true
  # TODO :: (0) insure lang path added
  assets:
    - assets/lang/
